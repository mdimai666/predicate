extends layouts/layout-index-nav

block content
  div.container
    h1= title
    hr
    
    //- img(src="/img.jpg", alt="")

    //- .alert.alert-info dima
    script.
      console.log(data);

    .row
      .col-xs-12
        -var e = {name: 'dima', body: 'text', footer: 'link'}
        +item(e)


//- mixins
mixin item(e)
  .panel.panel-default
    .panel-heading #{e.name}
    .panel-body #{e.body}
    .panel-footer=e.footer
